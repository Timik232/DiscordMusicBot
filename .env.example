# Discord Music Bot Environment Configuration
# Copy this file to .env and fill in your values

# Required: Discord Bot Token
# Get yours from https://discord.com/developers/applications
TOKEN=your_discord_bot_token_here

# Optional: Command prefix (default: /)
PREFIX=/

# Optional: Pipe mode max time in minutes (default: 10)
PIPE_MODE_MAX_TIME=10

# Optional: Storage path for sound files (default: ./storage)
# Note: In Docker, this is set to /app/storage
# STORAGE_PATH=./storage

# Proxy Configuration (for regions where Discord is blocked)
# Default is set to 192.168.1.47:2081 in docker-compose.yml
# Uncomment and modify if using a different proxy:
# HTTP_PROXY=http://192.168.1.47:2081
# HTTPS_PROXY=http://192.168.1.47:2081
# ALL_PROXY=http://192.168.1.47:2081

# Hosts to bypass proxy (comma-separated)
# NO_PROXY=localhost,127.0.0.1
